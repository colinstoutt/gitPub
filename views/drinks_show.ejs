<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>gitPub</title>
    <style>
      body {
        font-family: sans-serif;
        color: rgb(67, 67, 90);
      }
      h1 {
        text-align: center;
      }
      ul {
        text-align: center;
        list-style: none;
        padding: 0;
      }
      a {
        text-decoration: none;
        text-transform: uppercase;
        color: black;
        font-size: 20px;
        /* padding: 0; */
      }
      h2 {
        text-align: center;
        text-transform: uppercase;
      }
      h3 {
        text-align: center;
        font-size: 25px;
      }
      img {
        display: block;
        margin: auto;
        width: 300px;
        height: 300px;
        border-radius: 10px;
        box-shadow: rgba(0, 0, 0, 0.16) 0px 3px 6px,
          rgba(0, 0, 0, 0.23) 0px 3px 6px;
      }
      a {
        text-transform: none;
        display: block;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <h1>At foo bar</h1>
    <h2><%= drink.name%></h2>
    <img src="<%= drink.image %>" alt="<%= drink.name %>" />
    <h3>$<%= drink.price %></h3>
    <a href="http://localhost:3000/drinks/">See full list</a>
  </body>
</html>
